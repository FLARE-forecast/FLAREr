% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flare_io.R
\name{flare_get_folder_list}
\alias{flare_get_folder_list}
\title{List object keys under a prefix.}
\usage{
flare_get_folder_list(server_name = "", prefix = "", local_path = NULL, config)
}
\arguments{
\item{server_name}{Name of the DataStore (key under \code{config$s3}).}

\item{prefix}{Prefix to list under.}

\item{local_path}{Optional local filesystem directory to enumerate
when \code{mode="local"}. When supplied AND mode resolves to "local",
returns \code{list.files(local_path, recursive=TRUE, full.names=FALSE)}.
When NULL (default) AND mode="local", returns \code{character(0)}, so
remote-list callers iterate zero times in pure-local mode. Callers
that need an existence check that works under BOTH local and remote
modes (e.g. \code{get_run_config()}) should pass \code{local_path} so the
local enumeration is meaningful.}

\item{config}{FLAREr config list.}
}
\value{
Character vector of object keys / relative file paths.
}
\description{
List object keys under a prefix.
}
