% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_enkf_phy_update.R
\name{.diagnose_enkf_phy_update}
\alias{.diagnose_enkf_phy_update}
\title{Diagnose why PHY_TCHLA may not be updating individual phytoplankton states}
\usage{
.diagnose_enkf_phy_update(
  x_matrix,
  h,
  k_t,
  d_mat,
  ndepths_modeled,
  states_config
)
}
\arguments{
\item{x_matrix}{numeric matrix [nstate_cols x nmembers]}

\item{h}{numeric matrix [nobs_active x nstate_cols]}

\item{k_t}{numeric matrix [nstate_cols x nobs_active] — Kalman gain}

\item{d_mat}{numeric matrix [nobs_active x nmembers]}

\item{ndepths_modeled}{integer}

\item{states_config}{data frame with \code{state_names},
\code{states_to_obs}, and \code{states_to_obs_mapping} list columns}
}
\value{
invisibly NULL
}
\description{
Diagnose why PHY_TCHLA may not be updating individual phytoplankton states
}
\details{
Prints diagnostics at each DA step (when enabled) covering:
\enumerate{
\item \strong{H coefficients} — mapping coefficients the H matrix is actually
using for each PHY state.  Should match 12/Xcc from aed.nml.
\item \strong{Ensemble spread} — pre-update SD of each PHY state.  Near-zero SD
means the ensemble has collapsed; the filter cannot update without spread.
\item \strong{Innovation & predicted obs} — mean observed value, mean H\emph{x, ensemble
SD of H}x, and innovation (obs - H\emph{x).  Near-zero SD of H}x means K~0
regardless of obs_sd.  Near-zero innovation means no update is needed.
\item \strong{Kalman gain & update} — |K| and K*innovation for each PHY state.
\item \strong{Cross-state updates} — nutrients and other biologically coupled states
(NIT, PHS, SIL, OXY, CAR, OGM) driven by PHY_TCHLA obs via ensemble
covariance.  Cov sign reflects the current bloom phase, not a fixed rule.
\item \strong{Update decomposition} — PHY_TCHLA contribution vs all other active obs.
\item \strong{Cross-depth structure} — per-obs-depth update profile across state
depths, with cross-depth spreading percentage.
}
}
\section{Enabling}{

Set \code{options(flare.enkf.debug_phy = TRUE)} before calling
\code{run_flare()}.  Output goes to \code{message()}.
}

\keyword{internal}
