% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flare_io.R
\name{flare_arrow_s3_bucket}
\alias{flare_arrow_s3_bucket}
\title{Return an \code{arrow::s3_bucket()} (or local) handle for a partitioned
dataset under a server/prefix.}
\usage{
flare_arrow_s3_bucket(
  server_name = "",
  faasr_prefix = "",
  local_path = NULL,
  mode_override = NULL,
  config
)
}
\arguments{
\item{server_name}{Name of the DataStore (key under \code{config$s3}).}

\item{faasr_prefix}{Optional sub-prefix appended to the bucket path.}

\item{local_path}{Filesystem path used in \code{mode="local"}. Caller is
responsible for constructing this from any per-driver YAML
conventions (e.g. \code{config$met$future_met_model}). Ignored in
\code{mode="s3"}/\code{"faasr"}. If NULL in \code{mode="local"}, falls back to
\verb{<.flare_local_root(config)>/<server_name>/<faasr_prefix>}.}

\item{mode_override}{Optional explicit mode (\code{"local"}, \code{"s3"}, or
\code{"faasr"}) that bypasses \code{flare_io_mode(config)}. Used when a
per-driver YAML toggle (e.g. \code{config$met$future_met_use_s3},
\code{config$flows$use_flows_s3}) makes a specific driver local while
global outputs still go to S3. NULL (default) uses config-driven
dispatch.}

\item{config}{FLAREr config list.}
}
\value{
An \code{arrow::s3_bucket()} handle (modes \code{s3}/\code{faasr}) or
\code{arrow::SubTreeFileSystem} (mode \code{local}).
}
\description{
Used for \code{arrow::open_dataset()} / \code{arrow::write_dataset()} calls.
Note that under any mode, \code{arrow} operations on the returned handle
transit directly between R and the storage backend — they do not
funnel through the FaaSr RPC server. FaaSr (when in \code{mode="faasr"})
provides the credentials, but not the traffic path.
}
