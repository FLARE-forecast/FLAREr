% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_enkf_par_update.R
\name{.diagnose_enkf_par_update}
\alias{.diagnose_enkf_par_update}
\title{Diagnose parameter estimation performance in the EnKF}
\usage{
.diagnose_enkf_par_update(
  x_matrix,
  h,
  k_t,
  d_mat,
  ndepths_modeled,
  states_config,
  pars_config,
  n_non_vertical,
  npars,
  n_top = 5L
)
}
\arguments{
\item{x_matrix}{numeric matrix [nstate_cols x nmembers]}

\item{h}{numeric matrix [nobs_active x nstate_cols]}

\item{k_t}{numeric matrix [nstate_cols x nobs_active] — Kalman gain}

\item{d_mat}{numeric matrix [nobs_active x nmembers]}

\item{ndepths_modeled}{integer}

\item{states_config}{data frame with a \code{state_names} column}

\item{pars_config}{data frame with a \code{par_names} column}

\item{n_non_vertical}{integer number of non-vertical state variables
preceding the parameter block in the augmented state vector}

\item{npars}{integer number of estimated parameters}

\item{n_top}{integer number of top covariances/contributions to show per
parameter (default 5)}
}
\value{
invisibly NULL
}
\description{
Diagnose parameter estimation performance in the EnKF
}
\details{
For each estimated parameter in the augmented state vector, prints:
\itemize{
\item Pre-update ensemble mean and SD.
\item Net update decomposed into PHY_TCHLA contribution and all other active
observations.  If \code{net_upd} is dominated by \code{other_upd}, the
parameter is being driven mainly by temperature or oxygen obs rather than
chlorophyll.
\item Top state-depth covariances \code{Cov(par, state_depth)}: the pairs with
the largest ensemble cross-covariance identify what is driving the
parameter update.  The \code{obs} column shows which observation activates
each covariance path:
\itemize{
\item \code{PHY_TCHLA} — state maps to a composite chlorophyll obs row
\item \code{other}     — state is seen by a different obs type this step
\item \code{unobserved} — state has no active obs row; covariance exists
in the ensemble but cannot be used by the filter this step
}
\item Top active observation contributions \code{K[par,obs] * innov}: the
individual observations (ranked by |contribution|) that produce
\code{net_upd}.  Each row shows the observation label inferred from the
H matrix, the Kalman gain entry for this parameter, the mean innovation,
and the resulting contribution.  When high-covariance states are all
\code{unobserved}, the dominant contributions here come from observations
of different variable types (e.g. temperature driving a sediment-oxygen
parameter), which may indicate spurious cross-variable updating.
}
}
\section{Enabling}{

Set \code{options(flare.enkf.debug_phy = TRUE)} before calling
\code{run_flare()}.  Output goes to \code{message()}.
}

\keyword{internal}
