# Upload restart netcdf file to s3 bucket

Upload restart netcdf file to s3 bucket

## Usage

``` r
put_restart_file(saved_file, config)
```

## Arguments

- saved_file:

  full path of saved FLARE netcdf

- config:

  flare configuration object
