# Download restart file from s3 bucket

Download restart file from s3 bucket

## Usage

``` r
get_restart_file(config, lake_directory)
```

## Arguments

- config:

  flare configuration object

- lake_directory:

  full path to repository directory

## Value

list of updated configuration values
