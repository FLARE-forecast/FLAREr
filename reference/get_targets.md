# Download target data from s3

Download target data from s3

## Usage

``` r
get_targets(lake_directory, config = NULL)
```

## Arguments

- lake_directory:

  full path to the repository directory

- config:

  flare configuration object
