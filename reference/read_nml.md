# Read a GLM/ELCOM namelist file into an R list

Read a GLM/ELCOM namelist file into an R list

## Usage

``` r
read_nml(nml_file = "template")
```

## Arguments

- nml_file:

  Path to a `.nml` file.

## Value

A named list of class `"nml"` with one element per namelist block.
