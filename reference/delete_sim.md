# Delete simulation on s3 bucket

Delete simulation on s3 bucket

## Usage

``` r
delete_sim(site_id, sim_name, config)
```

## Arguments

- site_id:

  four letter code for site

- sim_name:

  name of simulation

- config:

  FLARE configuration object (needed for s3 buckets and endpoit)
