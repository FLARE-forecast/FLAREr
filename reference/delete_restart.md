# Delete restart file on s3 bucket

Delete restart file on s3 bucket

## Usage

``` r
delete_restart(site_id, sim_name, bucket = "restart", endpoint, config = NULL)
```

## Arguments

- site_id:

  four letter code for site

- sim_name:

  name of simulation

- bucket:

  s3 bucket

- endpoint:

  S3 endpoint
